<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Author</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <% @articles.each do |article| %>
        <tr>
            <td><%=article.title%></td>
            <td><%=article.descrition%></td>
            <td><%=article.author%></td>
            <td><%= link_to "Show", article_path(article)%></td>
            <td><%= button_to "Destroy this article",  article_path(article.id), method: :delete %></td>
        </tr>
        <%end%>

    </tbody>
</table>
<%= link_to "Create Article", path=new_article_path %>